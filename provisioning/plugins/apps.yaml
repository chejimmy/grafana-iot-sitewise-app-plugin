apiVersion: 1

apps:
  - type: 'grafana-grafanaiotsitewise-app'
    org_id: 1
    org_name: 'Grafana'
    disabled: false
    jsonData:
      apiUrl: http://default-url.com
    secureJsonData:
      apiKey: secret-key
